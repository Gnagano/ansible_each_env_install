python_env_install_dir: "{{ anyenv_root | default('/opt/anyenv') }}/envs/pyenv"
ruby_env_install_dir: "{{ anyenv_root | default('/opt/anyenv') }}/envs/rbenv"
php_env_install_dir: "{{ anyenv_root | default('/opt/anyenv') }}/envs/phpenv"

python_version: 2.7.15
ruby_version: 2.2.0
php_version: 5.6.37

install_envs:
  - python
  - ruby
  - php
